apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "devops-chart.fullname" . }}-envs
  labels:
    {{- include "devops-chart.labels" . | nindent 4 }}
data:
  {{- range .Values.environmentVariables }}
  {{ .name }}: "{{ .value }}"
  {{- end }}
